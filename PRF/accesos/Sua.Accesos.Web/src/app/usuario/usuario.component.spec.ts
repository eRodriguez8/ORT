import { UsuarioComponent } from './usuario.component';

describe ('UsuarioComponent', () => {
  const component = UsuarioComponent;

  it('should get usuario', () => {
    expect(component).toBeTruthy();
  });
});

